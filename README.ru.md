# 📋 ru-it-audit-notebook

[![CI Status](https://github.com/ranas-mukminov/ru-it-audit-notebook/actions/workflows/ci.yml/badge.svg)](https://github.com/ranas-mukminov/ru-it-audit-notebook/actions)
[![License](https://img.shields.io/badge/License-Apache%202.0-blue.svg)](LICENSE)
[![Python](https://img.shields.io/badge/python-3.10+-blue.svg)](https://www.python.org/downloads/)

🇷🇺 Русская версия | 🇬🇧 [English version](README.md)

**Автор**: [Ранас Мукминов](https://github.com/ranas-mukminov)  
**Сайт**: [run-as-daemon.ru](https://run-as-daemon.ru)

---

## Описание

`ru-it-audit-notebook` — это открытый интерактивный генератор отчётов по ИТ-аудиту, адаптированный под российские бизнес-контексты. Проект объединяет Jupyter-ноутбуки и YAML-чек-листы для создания профессиональных HTML- и PDF-отчётов в ключевых областях: защита персональных данных (152-ФЗ), резервное копирование, критическая информационная инфраструктура (КИИ) и бизнес-приложения вроде 1С.

Инструмент трансформирует данные инвентаризации и ответы на вопросники в кратких executive summary для руководства и детализированные технические приложения для инженеров. Предназначен для DevOps/DevSecOps команд, внутренних аудиторов и ИТ-интеграторов, автоматизирует регулярные аудиторские процессы, но не заменяет формальных юридических экспертиз.

## Основные возможности

- **Чек-листы по комплаенсу**: Готовые YAML-шаблоны для 152-ФЗ (персональные данные), резервирования, базовых требований КИИ и ландшафта приложений 1С
- **Workflow на базе Jupyter**: Интерактивные ноутбуки парсят CSV-инвентаризацию, оценивают контроли и генерируют Markdown-секции
- **Двойной формат отчётов**: Executive summary для руководителей и технические детали для ИТ-персонала, рендеринг в HTML/PDF через `nbconvert`
- **Scoring и оценка рисков**: Настраиваемая весовая оценка с порогами `ok`/`warning`/`critical` для каждого контроля
- **AI-ready заглушки**: Опциональные хуки для AI-генерируемых резюме и рекомендаций по плану работ (по умолчанию no-op провайдер для безопасности данных)
- **Российский контекст**: Все чек-листы, терминология и примеры адаптированы под российские регуляторные требования и инструментарий (1С и др.)
- **Готовность к автоматизации**: CLI-команды для пакетной обработки инвентаризаций и сборки отчётов в CI/CD пайплайнах

## Архитектура и компоненты

Проект состоит из трёх основных слоёв:

1. **Ядро — Python-библиотека** (`it_audit_report/`):
   - `checklist_loader.py`: Загрузка и валидация YAML-чек-листов.
   - `scoring.py`: Расчёт весовых оценок и категоризация находок.
   - `inventory/`: Парсинг CSV-инвентаризаций серверов, сервисов и приложений.
   - `report/`: Рендеринг Markdown на базе Jinja2 для executive и технических секций.
   - `ai/`: Опциональные заглушки для AI-интеграции (по умолчанию NoopAIProvider).

2. **YAML-чек-листы** (`checklists/`):
   - `pdn_152fz.yaml`, `backup_resilience.yaml`, `kii_basic.yaml`, `app_1c.yaml`, `inventory.yaml`.
   - Каждый определяет контроли (ID, название, описание, веса scoring, ссылки на артефакты).

3. **Jupyter-ноутбуки** (`notebooks/`):
   - `00_inventory_servers.ipynb` — загрузка инвентаризации серверов.
   - `01_pdn_152fz_checklist.ipynb` — контроли по персональным данным.
   - `02_backup_resilience_checklist.ipynb` — оценка резервного копирования и DR.
   - `03_kii_basic_checklist.ipynb` — базовые вопросы КИИ.
   - `04_applications_1c_business.ipynb` — ландшафт 1С.
   - `99_report_assembler.ipynb` — сборка всех выходов в финальный Markdown.

**Поток данных**:  
CSV инвентаризации + Ответы (YAML/JSON) → Ноутбуки (scoring + контекст) → Шаблоны Jinja2 → Markdown → `nbconvert` → HTML/PDF отчёт

## Требования

### Операционные системы
- **Рекомендуется**: Ubuntu 22.04+ / Debian 11+, Rocky Linux 8+ или любой современный Linux-дистрибутив с Python 3.10+
- **Windows/macOS**: Поддерживается через виртуальные окружения Python

### Программные зависимости
- **Python**: 3.10 или новее
- **pip**: Рекомендуется последняя версия
- **Пакеты**: Устанавливаются автоматически через `pip install -e .`
  - `typer`, `pyyaml`, `jinja2`, `pandas`, `papermill`, `nbformat`, `nbconvert`

### Опционально для разработки
- **Инструменты разработки**: `pytest`, `mypy`, `ruff`, `bandit`, `pip-audit` (устанавливаются с `[dev]`)
- **Конвертация в PDF**: `pandoc` и LaTeX (например, `texlive-xetex`) для вывода в PDF

### Системные ресурсы
- **CPU/RAM**: Минимальные; ноутбуки обрабатывают небольшие датасеты (сотни контролей/серверов)
- **Диск**: ~50 МБ для кода и зависимостей, плюс место для артефактов

### Права доступа
- **Пользователь**: Не требуется root/sudo для установки и обычного использования
- **Сеть**: Доступ в интернет для установки пакетов через `pip`; во время работы сетевых вызовов по умолчанию нет

## Быстрый старт

```bash
# 1. Клонируйте репозиторий
git clone https://github.com/ranas-mukminov/ru-it-audit-notebook.git
cd ru-it-audit-notebook

# 2. Установите пакет в режиме разработки
pip install -e .[dev]

# 3. Запустите ноутбуки с примерами данных
ru-it-audit run-notebooks \
  --inventory examples/inventory/sample_inventory.csv \
  --answers-dir examples/inputs \
  --out-dir .artifacts/notebooks

# 4. Соберите HTML- и PDF-отчёты
ru-it-audit build-report \
  --artifacts-dir .artifacts/notebooks \
  --out-html .artifacts/report.html \
  --out-pdf .artifacts/report.pdf

# 5. Откройте отчёт
xdg-open .artifacts/report.html  # или откройте .artifacts/report.pdf
```

Креденшиалы/URL по умолчанию: не применимо (генератор статических отчётов).

## Детальная установка

### Установка на Ubuntu / Debian

```bash
# Обновите индекс пакетов и установите Python 3.10+
sudo apt update
sudo apt install -y python3 python3-pip python3-venv

# Клонируйте репозиторий
git clone https://github.com/ranas-mukminov/ru-it-audit-notebook.git
cd ru-it-audit-notebook

# Создайте и активируйте виртуальное окружение (рекомендуется)
python3 -m venv venv
source venv/bin/activate

# Установите пакет с зависимостями для разработки
pip install --upgrade pip
pip install -e .[dev]

# Опционально: установите LaTeX для генерации PDF
sudo apt install -y pandoc texlive-xetex texlive-fonts-recommended texlive-lang-cyrillic
```

### Установка на RHEL / Rocky / AlmaLinux

```bash
# Установите Python 3.10+ (RHEL 8+ включает модуль python39 или python3.11)
sudo dnf install -y python3.11 python3.11-pip

# Клонируйте и перейдите в директорию
git clone https://github.com/ranas-mukminov/ru-it-audit-notebook.git
cd ru-it-audit-notebook

# Создайте виртуальное окружение
python3.11 -m venv venv
source venv/bin/activate

# Установите
pip install --upgrade pip
pip install -e .[dev]

# Опционально: LaTeX
sudo dnf install -y pandoc texlive-xetex
```

### Установка с Docker (опционально)

Если предпочитаете контейнеризованное выполнение:

```bash
# Соберите Docker-образ
docker build -t ru-it-audit-notebook:latest .

# Запустите ноутбуки в контейнере (примонтируйте локальные данные)
docker run --rm \
  -v $(pwd)/examples:/data \
  -v $(pwd)/.artifacts:/output \
  ru-it-audit-notebook:latest \
  ru-it-audit run-notebooks \
    --inventory /data/inventory/sample_inventory.csv \
    --answers-dir /data/inputs \
    --out-dir /output/notebooks
```

> **Примечание**: Dockerfile отсутствует в текущем репозитории; вам нужно создать простой образ на базе Python с установкой `ru-it-audit-notebook`.

## Настройка

### Формат CSV-инвентаризации

Файл `--inventory` должен быть CSV с колонками, соответствующими вашей инфраструктуре:

```csv
hostname,ip,os,role,location,owner
app-db-01,10.0.1.10,Ubuntu 22.04,database,DC-Moscow,IT-Team
web-frontend,10.0.2.5,Debian 11,web,DC-SPB,DevOps
1c-server,192.168.1.20,Windows Server 2019,1C,Office,Accounting
```

При необходимости адаптируйте названия колонок; ноутбуки читают через `pandas.read_csv()`.

### Структура директории с ответами

Файлы ответов могут быть в формате YAML или JSON, именованные по чек-листам:

```
examples/inputs/
  pdn_152fz_answers.yaml
  backup_resilience_answers.yaml
  kii_basic_answers.yaml
  app_1c_answers.yaml
```

**Пример файла ответов** (`pdn_152fz_answers.yaml`):

```yaml
# Значения ответов должны совпадать с 'answer_type' и ключами 'scoring' в чек-листе
PDN-001: true              # boolean: назначен оператор
PDN-002: "complete"        # choice: состояние реестра
PDN-003: "full"            # choice: покрытие правовых оснований
PDN-004: 3                 # scale: уровень документированных контролей
PDN-005: true              # boolean: соблюдение локализации
PDN-006: 2                 # scale: журналирование и контроль доступа
```

### Профили scoring

Редактируйте `checklists/scoring_profiles.yaml` для настройки глобальных весов и порогов:

```yaml
profiles:
  strict:
    weight_multiplier: 1.2
    critical_threshold_pct: 10  # Любой контроль >10% critical → общий провал
  relaxed:
    weight_multiplier: 0.8
    critical_threshold_pct: 25
```

Применяйте профиль через CLI (функция в разработке) или редактируйте умолчания в `it_audit_report/config.py`.

### Переменные окружения

Установите для настройки поведения:

```bash
export AUDIT_NOTEBOOK_AI_PROVIDER="openai"  # или "noop" (по умолчанию)
export AUDIT_NOTEBOOK_AI_MODEL="gpt-4"
export AUDIT_NOTEBOOK_AI_API_KEY="sk-..."
```

Функциональность AI опциональна; по умолчанию `NoopAIProvider` не делает внешних вызовов.

## Использование и типовые задачи

### Запуск всех ноутбуков

```bash
ru-it-audit run-notebooks \
  --inventory examples/inventory/sample_inventory.csv \
  --answers-dir examples/inputs \
  --out-dir .artifacts/notebooks
```

Выполняет все ноутбуки в `notebooks/` через `papermill`, инжектируя параметры и сохраняя результаты.

### Сборка финального отчёта

```bash
ru-it-audit build-report \
  --artifacts-dir .artifacts/notebooks \
  --out-html .artifacts/report.html \
  --out-pdf .artifacts/report.pdf
```

Конвертирует результаты ноутбуков в Markdown, затем в HTML/PDF через `nbconvert`.

### Доступ к веб-интерфейсу

Не применимо (это статический генератор). Откройте сгенерированный HTML в браузере:

```bash
xdg-open .artifacts/report.html
# или
firefox .artifacts/report.html
```

### Добавление нового чек-листа

1. Создайте `checklists/my_checklist.yaml`, следуя схеме из существующих файлов.
2. Добавьте соответствующий ноутбук `notebooks/05_my_checklist.ipynb`.
3. Загрузите и оцените в ноутбуке:

```python
from it_audit_report.checklist_loader import load_checklist
from it_audit_report.scoring import score_checklist

checklist = load_checklist("checklists/my_checklist.yaml")
results = score_checklist(checklist, answers)
print(results)
```

4. Обновите `99_report_assembler.ipynb`, чтобы включить новую секцию.

### Валидация YAML-чек-листов

```bash
yamllint checklists/*.yaml
```

Или используйте CLI-валидатор (если реализован):

```bash
ru-it-audit validate-checklist checklists/pdn_152fz.yaml
```

## Обновление

### Обновление из Git

```bash
cd ru-it-audit-notebook
git pull origin main
pip install -e .[dev]  # переустановите на случай изменения зависимостей
```

### Обновление зависимостей Python

```bash
pip install --upgrade -e .[dev]
```

### Breaking Changes

- **v0.1 → v0.2 (будущее)**: Схема чек-листов может получить обязательные поля; смотрите руководство по миграции в `CHANGELOG.md`.

## Логи, мониторинг и устранение неполадок

### Расположение логов

- **Логи CLI**: Выводятся в stdout/stderr; перенаправляйте с помощью `ru-it-audit run-notebooks 2>&1 | tee audit.log`
- **Выполнение ноутбуков**: Papermill сохраняет логи ячеек в `.artifacts/notebooks/*.ipynb` (выполненные ноутбуки)

### Распространённые проблемы

#### Ноутбуки не запускаются

**Симптом**: `FileNotFoundError: inventory file not found`

**Решение**:
```bash
# Проверьте, что путь к инвентаризации совпадает с аргументом CLI
ls -lh examples/inventory/sample_inventory.csv
# Убедитесь, что он передан правильно
ru-it-audit run-notebooks --inventory examples/inventory/sample_inventory.csv ...
```

#### Не генерируется PDF

**Симптом**: Ошибка `nbconvert` или отсутствуют шрифты в PDF

**Решение**:
```bash
# Установите LaTeX-бэкенд
sudo apt install -y pandoc texlive-xetex texlive-fonts-recommended texlive-lang-cyrillic
# Если отсутствуют кириллические шрифты:
sudo apt install -y fonts-liberation fonts-dejavu
```

#### Нет данных в отчёте / пустые оценки

**Симптом**: Отчёт показывает "No controls evaluated"

**Решение**:
- Проверьте, что имена файлов ответов совпадают с ID чек-листов.
- Убедитесь, что `answers_dir` содержит файлы `*_answers.yaml`.
- Изучите результаты ноутбуков в `.artifacts/notebooks/` на предмет ошибок.

#### Ошибки импорта

**Симптом**: `ModuleNotFoundError: No module named 'it_audit_report'`

**Решение**:
```bash
# Убедитесь, что пакет установлен в режиме разработки
pip install -e .
# Или активируйте виртуальное окружение, если вы его создали
source venv/bin/activate
```

### Включение режима отладки

Установите переменную окружения для подробного вывода CLI:

```bash
export AUDIT_NOTEBOOK_DEBUG=1
ru-it-audit run-notebooks ...
```

## Замечания по безопасности

### Минимальный чек-лист безопасности

- **Защита чувствительных данных**: Не коммитьте реальные CSV-инвентаризации или файлы ответов с продакшн-именами хостов/IP в публичные репозитории.
- **Безопасность AI-провайдера**: По умолчанию `NoopAIProvider` не делает внешних API-вызовов. Включайте AI-функции только если доверяете провайдеру и изучили политику обмена данными.
- **Конфиденциальность отчётов**: Сгенерированные HTML/PDF отчёты могут содержать чувствительные детали инфраструктуры. Храните и передавайте безопасно (шифрование, ограничение доступа).
- **Гигиена зависимостей**: Регулярно запускайте `pip-audit` и `bandit` для обнаружения известных уязвимостей в зависимостях (см. `scripts/security_scan.sh`).

### Рекомендации

- Используйте приватные Git-репозитории для аудиторских данных.
- Шифруйте артефакты отчётов на диске (например, LUKS-тома, VeraCrypt).
- Ограничьте доступ к директориям `.artifacts/` и `examples/` (при необходимости установите `chmod 700`).
- Проверяйте AI-генерируемый контент вручную перед включением в официальные отчёты.

> **Юридический дисклеймер**: Этот инструмент не предоставляет юридических консультаций или сертификации. Всегда консультируйтесь с юристами и специалистами по комплаенсу по вопросам 152-ФЗ, КИИ и других регуляций.

## Структура проекта

```
ru-it-audit-notebook/
├── checklists/               # YAML-определения контролей комплаенса
│   ├── pdn_152fz.yaml       # Персональные данные (152-ФЗ)
│   ├── backup_resilience.yaml
│   ├── kii_basic.yaml       # Критическая информационная инфраструктура
│   └── app_1c.yaml          # Ландшафт приложений 1С
├── it_audit_report/          # Ядро — Python-библиотека
│   ├── checklist_loader.py
│   ├── scoring.py
│   ├── models.py
│   ├── inventory/           # Парсинг CSV
│   ├── report/              # Генерация Markdown
│   ├── ai/                  # Заглушки AI-интеграции
│   └── cli/                 # CLI-команды Typer
├── notebooks/                # Jupyter-ноутбуки для выполнения
│   ├── 00_inventory_servers.ipynb
│   ├── 01_pdn_152fz_checklist.ipynb
│   └── 99_report_assembler.ipynb
├── examples/                 # Примеры данных для тестирования
│   ├── inventory/
│   ├── inputs/
│   └── outputs/
├── templates/                # Jinja2-шаблоны для отчётов
├── tests/                    # pytest unit и integration тесты
├── scripts/                  # CI/CD и вспомогательные скрипты для разработки
│   ├── lint.sh
│   ├── security_scan.sh
│   └── dev_run_all_tests.sh
├── .github/workflows/        # GitHub Actions CI
├── pyproject.toml            # Метаданные проекта и зависимости
├── CONTRIBUTING.md
├── LEGAL.md
└── LICENSE                   # Apache-2.0
```

## Планы развития

- **v0.2**: Добавить CLI-команду `validate-checklist` для валидации схемы.
- **v0.3**: Поддержка дополнительных чек-листов (профили ФСТЭК ГосСОПКА, маппинг ISO 27001).
- **v0.4**: Веб-интерфейс для интерактивного заполнения вопросников (на базе Flask/FastAPI).
- **v0.5**: Интеграция с системами управления активами (API-коннекторы к популярным CMDB/ITSM).

Приглашаем к участию! См. [CONTRIBUTING.md](CONTRIBUTING.md) для правил.

## Как внести вклад

Приветствуем контрибуции! Для поддержания качества и безопасности:

### Как открыть issue

- Используйте GitHub Issues: [https://github.com/ranas-mukminov/ru-it-audit-notebook/issues](https://github.com/ranas-mukminov/ru-it-audit-notebook/issues)
- Предоставьте минимальный воспроизводимый пример (CSV инвентаризации, YAML ответов, CLI-команда).
- Избегайте публикации чувствительных продакшн-данных; анонимизируйте имена хостов и IP.

### Как предложить Pull Request

1. Сделайте fork репозитория.
2. Создайте feature-ветку: `git checkout -b feature/new-checklist`.
3. Сначала напишите тесты (обязателен TDD-подход для основной логики).
4. Реализуйте изменения.
5. Запустите полный набор тестов: `./scripts/dev_run_all_tests.sh`.
6. Убедитесь, что линтинг проходит: `./scripts/lint.sh`.
7. Коммитьте с понятными сообщениями: `git commit -m "Add КИИ extended checklist"`.
8. Запушьте и откройте PR в ветку `main`.

### Основы стиля кода

- **Python**: PEP 8, type hints везде, используйте `ruff` для форматирования и линтинга.
- **YAML**: Используйте `yamllint` для валидации файлов чек-листов.
- **Ноутбуки**: Держите ячейки минимальными и воспроизводимыми; избегайте деструктивных действий.
- **Безопасность**: По умолчанию `NoopAIProvider`; требуется явное согласие для внешних сервисов.
- **Документация**: Обновляйте и `README.md`, и `README.ru.md` при изменениях, влияющих на пользователей.

См. [CONTRIBUTING.md](CONTRIBUTING.md) для детального описания workflow.

## Лицензия

Проект лицензирован под **Apache License 2.0**.  
См. полный текст в файле [LICENSE](LICENSE).

Вы можете использовать это ПО коммерчески, модифицировать и распространять при условии сохранения первоначальных уведомлений о лицензии.

## Автор и коммерческая поддержка

**Автор**: [Ранас Мукминов](https://github.com/ranas-mukminov)  
**Сайт**: [run-as-daemon.ru](https://run-as-daemon.ru)

Для продакшн-внедрения, кастомизации чек-листов, интеграции с вашими CMDB/ITSM и профессиональных аудиторских услуг доступна коммерческая поддержка:

- **ИТ-аудит инфраструктуры**: Серверы, ландшафты 1С, облака (Яндекс.Облако, VK Cloud и др.)
- **Оценка комплаенса**: 152-ФЗ, КИИ, стандарты ФСТЭК
- **Регулярная отчётность**: Автоматизированные процессы, интеграция в CI/CD, дашборды для руководства
- **Обучение**: Воркшопы для внутренних команд аудита и DevSecOps-инженеров

Посетите [https://run-as-daemon.ru](https://run-as-daemon.ru) или свяжитесь через GitHub-профиль для запросов.

---

**Благодарности**: Спасибо open-source сообществу за Jupyter, Pandas и инструментарий Python, позволивший создать этот проект.
